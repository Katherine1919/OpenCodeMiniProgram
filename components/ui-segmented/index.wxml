<view class="ui-segmented">
  <view
    class="segmented-item {{index === selected ? 'active' : ''}}"
    wx:for="{{options}}"
    wx:key="text"
    data-index="{{index}}"
    bindtap="onSelect"
  >
    <text>{{item.text}}</text>
  </view>
</view>
